apiVersion: v1
kind: Service

metadata:
  name: student-registration-frontend
  namespace: student-registration

spec:
  type: ClusterIP

  selector:
    app: student-registration-frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
